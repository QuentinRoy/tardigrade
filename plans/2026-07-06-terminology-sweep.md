# Terminology sweep: apply the settled vocabulary across code, DB, routes, and UI

- **Status:** Active
- **Created:** 2026-07-06
- **Origin:** #99 terminology convergence; `CONTEXT.md`, `docs/reference/lexicon.md`
- **Tracked by:** #99 (and #136 for the submission → Grade Target portion)

The vocabulary is settled in `CONTEXT.md` (internal domain glossary) and `docs/reference/lexicon.md` (user-facing). This plan applies those decisions to the implementation, staged so each step is independently reviewable and shippable. Mostly mechanical application — but grilling stage 1 (2026-07-07) surfaced one genuine terminology decision the sweep had to make: the grid's summary surfaces (**Dashboard**, "Rubric overview", "Analytics") were near-synonyms. Resolved to **Overview** (grid home — retires "Dashboard") vs **Results** (the outcomes page holding the **Grades** matrix + **Analytics** breakdown); recorded in the lexicon.

**Don't leak this plan's own labels into code comments.** Terms like "stage 2b", "terminology sweep", a grilling question id (`Q4`), or a finding number are meaningless to a reader who doesn't have this plan open — and this plan is temporary (`plans/` lifecycle: `Active` → `Completed`/`Abandoned`, then it stops being anyone's reference). A code comment must stand on its own: state the actual technical reason (what invariant, what constraint, what behavior would otherwise surprise a reader), not a pointer into this document. If a comment genuinely cannot be self-contained without this plan's context, link the specific file (`plans/2026-07-06-terminology-sweep.md`) rather than naming a stage in prose — but that should be rare; usually the self-contained technical reason is what belongs in the comment anyway.

## State of the code (2026-07-06, after the MUI → Mantine merge #242)

The Mantine migration independently and **partially** adopted some of the new vocabulary, leaving the tree in a mixed state the sweep must reconcile — not a clean single-direction rename from the old terms:

- Already renamed toward the target: `src/rubrics/{Boolean,Numerical,Ordinal}GradeControl.tsx` (Grade), `src/rubrics/RubricCriterion.tsx` (was `RubricGradeRow`), `src/assessment-capture/RubricGradeList.tsx`.
- Still old, sometimes beside the renamed files: `src/rubrics/AssessmentStatus.tsx` (sits next to the `*GradeControl` files), and all of `SubmissionMatrix`, `RubricAnalyticsTable`, `QuestionDetailsTooltip`, the `assessment-*` / `submission*` / `Question*` names, plus the entire schema.

So a few "old term" starting points below have already moved, and the sweep additionally has to make the half-migrated component names internally consistent (e.g. `AssessmentStatus` → a Grade-named equivalent; confirm `RubricCriterion` reads correctly once the leaf is `Criterion`).

## Decided renames

| Old | New | Surfaces |
| --- | --- | --- |
| Project | Grid | table, `id`/`row_id`, routes (`/projects` → `/grids`), cache tags, code identifiers, UI |
| Question | Rubric | table, FKs, code, UI, YAML import (`questions:` → `rubrics:`) |
| Rubric (leaf) | Criterion | tables (`*_rubric*`), FKs, code, UI, CSV columns |
| Team | Group | table, FKs, code, UI, students CSV (`team` → `group`) |
| Submission | Grade Target | table, FKs, `id`/`row_id`, code; **not** user-facing (UI/URLs name the Student or Group; `[targetId]` param only) |
| Assessment (record + act) | Grade | tables (`assessment`, `rubric_assessment`, `*_rubric_assessment`), `saveAssessment`, code, UI |
| Assessment Completion | Grade Completion | code, projections |
| Submission Matrix | Grade Matrix (internal) / "Grades" (UI) | component, aria-label, UI |
| Rubric Analytics | Criterion Analytics (internal) / "Analytics" (section on Results) | component, UI |
| Rubric overview (page) | **Results** page (Grades + Analytics); grid home "Dashboard" → **Overview** | page titles, routes, nav |
| Marks (aggregate) | Total | export columns (`grand_total_marks` → `final_total`), code |
| progress (as a synonym for completion) | Completion | `progress`/`progressPromise`/`progressLabel` props, `CompletionProgress` component, UI copy — unify on Completion (the concept `CONTEXT.md` settled) |
| boolean / ordinal / numerical (criterion kinds) | Check / Options / Number | `criterion_kind` DB enum (was `rubric_type`), subtype tables (`boolean_rubric*` → `check_criterion*` etc.), code, YAML `kind:` field + values, UI kind selector — one vocabulary end to end, no internal/external split. The classifier word is `kind`, matching the grade row's `kind` column (never "type"). **Folded into stage 2a** (shares the leaf rename's subtype-table migration — one hop, tables touched once). |

Value pipeline is **Grade → Mark → Total** for every criterion kind; a grade's recorded content varies by kind (a Yes/No answer, a label, or — for Number criteria only — a value). `grade` is the record and the act, distinct from its worth (a Number grade is itself a number, but never the marks value); `value` (never "score") names a Number grade's payload and the input axis of its configuration (`minValue..maxValue`), not a pipeline stage. `Points` remains avoided.

## Routes (final shape)

```
/grids/[gridId]/[gridSlug]/                                 (Overview — grid home, was "Dashboard")
/grids/[gridId]/[gridSlug]/rubrics/
/grids/[gridId]/[gridSlug]/grades/
/grids/[gridId]/[gridSlug]/grades/[targetId]/[targetSlug]/
/grids/[gridId]/[gridSlug]/grades/[targetId]/[targetSlug]/rubrics/[rubricId]/
/grids/[gridId]/[gridSlug]/results/                         (Results — Grades + Analytics, was "/analytics/")
/grids/[gridId]/[gridSlug]/import/...
```

## CSV columns

- students: `team` → `group`.
- grades: `submission_type` → `kind` (`individual`/`group`), `submitter` → `name`, assessment columns `questionId:rubricId` → `rubricId:criterionId`, `grand_total_marks` → `final_total`, per-question totals → per-rubric `rubricId:total`.

## Staging

Each stage is one or more PRs, own migration where schema changes, Kysely types regenerated, tests updated. Order minimizes cross-stage churn (rename leaf-inward and identifier-outward). Per `docs/reference/database-migrations.md`, committed migrations are not rewritten — every schema rename is a new migration.

**Sweep scope — include `e2e/`.** Any rename sweep must cover `e2e/` (specs and `e2e/fixtures/`), not just `src/` and `app/`. The e2e specs drive the real UI: they match on-screen labels (e.g. the import form's `Rubrics YAML` field, the `Rubrics assessed` completion tile), navigate literal route paths (`/import/rubrics`), and load fixture files whose top-level keys are contract-shaped (`rubrics:`). A rename that skips `e2e/` compiles and passes unit/integration but breaks `test:e2e` with stale-label timeouts, and fixture files can silently drift from the wire format. Stage 2b hit exactly this — `e2e/fixtures/questions.yaml` and the spec's `Questions YAML`/`/import/questions`/`Questions assessed` were missed on the first pass. Also watch **ALL-CAPS constants** (`QUESTIONS_YAML_PLACEHOLDER`): a case-sensitive `s/Question/Rubric/; s/question/rubric/` pass won't catch `QUESTIONS`, so add an explicit all-caps rule or grep for the upper-cased term afterward.

**Validation per stage — run the full gate, including `test:e2e`.** The complete gate is `check --fix`, `check-types`, `lint:boundaries`, the full `vitest` suite (unit + integration + storybook), production `build`, **and `test:e2e`** (it spins up its own ephemeral Postgres + built app and exercises import → grade → persist → export end-to-end — the only check that catches stale UI labels, routes, and fixture drift). `test:e2e` is not optional for these sweeps; a green unit/integration run is not sufficient. Note the storybook project can flake under the combined three-project `vitest` run (runner contention); re-run `test:storybook` in isolation to confirm before treating a storybook failure as a real regression.

1. **Results page + analytics module** — self-contained, TypeScript-only (no schema change): rename the readout page to **Results** and reduce it to its two tables. Adopts final Grade-Target/Rubric/Criterion names *inside this module* ahead of the schema stages (it imports the not-yet-renamed `#questions`/`#rubrics`/`#submissions`, which stay old until their stages); the assess→grade axis is untouched here (stage 5) — fields keep "assessed"/"assessment".
   - **Components:** `SubmissionMatrix` → `GradeMatrix` (renders the **Grades** table; aria-label/heading "Grades" — note "Grade Matrix" per `CONTEXT.md`, *not* "GradeTargetMatrix"), `RubricAnalyticsTable` → `CriterionAnalyticsTable` (heading "Analytics").
   - **Page** (`app/.../assessments/overview/page.tsx`): title "Rubric overview" → **"Results"**; section headings "Rubric analytics" → **"Analytics"**, "Submission matrix" → **"Grades"**. Nav-drawer item "Rubric overview" → "Results". `projectOverviewPath` → `projectResultsPath` (frees `projectOverviewPath` for the Dashboard→Overview follow-up, stage 8).
   - **Remove the page-level summary strip entirely** (graded-count, completion, average-total — all duplicated by the grid home). This deletes `RubricOverviewSummary`, the `summary` projection, and the `classAverage*` fields. Consequences: former **bug-fix (b)** (relabel "Class average" → "Average total") is **mooted** (deleted, not relabeled), and the `assessedRubrics`/`totalRubrics` → `assessedCriteria`/`totalCriteria` rename is **mooted** (both were summary-only fields). Trim the corresponding `summary` assertions from the builder tests.
   - **Bug-fix (a), kept:** the Grades table's per-row "Average" column renders `marks/maxMarks` — a target's **Total**, not a mean — relabel to **Total**. The Criterion Analytics "Average" column is a real per-criterion mean and stays.
   - **Internal field renames:** `submissionId`/`submissionLabel` → `gradeTargetId`/`label`, `questionId`/`questionLabel` → `rubricId`/`rubricLabel`, leaf `rubricId` → `criterionId` (note this *reuses* `rubricId` for the container that `questionId` named — sequence the rename to avoid a half-state where `rubricId` means both).
   - **Folder + `*Overview*` symbol-family rename** — folder `src/rubric-analytics/` → `src/results/`. The "overview" prefix named *this page's data*, so it becomes `Results`: `rubricOverviewBuilder.ts`/`loadRubricOverview.ts` → `resultsBuilder.ts`/`loadResults.ts`; `RubricOverviewData`/`buildRubricOverviewData`/`loadRubricOverviewData`/`rubricOverviewCacheTags` → `ResultsData`/`buildResultsData`/`loadResultsData`/`resultsCacheTags`. Row types named for what each row *is*: `RubricOverviewRow` (per-leaf) → `CriterionRow`, `RubricOverviewSubmissionRow` (per-target) → `GradeTargetRow` (`.rubrics` → `.criteria`, `completedRubrics`/`totalRubrics` → `completedCriteria`/`totalCriteria`), `RubricOverviewSubmissionCell` → `GradeTargetCell`. `RubricOverviewAssessmentRecord`/`loadRubricAssessmentRecordsFromDb` → `ResultsAssessmentRecord`/`loadCriterionAssessmentRecordsFromDb` (keep "Assessment" — stage 5); its `select` aliases rename (`… as gradeTargetId`, `… as criterionId`) while the SQL table/column identifiers stay old. Tooltip swap: `RubricDetailsTooltip` (leaf) → `CriterionDetailsTooltip`, then `QuestionDetailsTooltip` (container) → `RubricDetailsTooltip`. `CompletionProgress`/`MarksBadge.module.css` unchanged.
2. **Question → Rubric / Rubric → Criterion (criterion-*kind* rename folded in)** — schema (tables, FKs, triggers, enum), Kysely types, code, YAML import, UI. Largest leaf rename; do before the submission and grade renames so those build on final names. Split into **two PRs, leaf-first**, so the word `rubric` / identifier `rubricId` is fully vacated from the leaf before the container claims it — the same reuse hazard as stage 1's tooltip swap; never let `rubricId`/`rubric` mean both leaf and container at once. (Grilled 2026-07-07.)
   - **2a — leaf `Rubric` → `Criterion`, with the criterion-*kind* rename folded in.** The kind axis rides here, not a separate stage, because it shares the subtype-table migration: the subtype tables go straight to their final names in **one hop, touched once** — `boolean_rubric*` → `check_criterion*`, `ordinal_rubric*` → `options_criterion*`, `numerical_rubric*` → `number_criterion*`; enum `rubric_type` → `criterion_kind`, column `type` → `kind`, values `boolean`/`ordinal`/`numerical` → `check`/`options`/`number`. **Landmine — do not mechanically produce `options_criterion_value`:** "value" is reserved for the Number payload (CONTEXT `Value`; stage 7b). `ordinal_rubric_value` becomes **`options_criterion_mark`** (type `OrdinalRubricValue` → `OptionsCriterionMark`, FK `ordinalRubricId` → `optionsCriterionId`, cols `label`/`marks` unchanged) — each row is one option's mark entry (CONTEXT "Options Marks Minimum … mark entries"). Folder `src/rubrics/` → **`src/criteria/`** (the `"#*": "./src/*"` wildcard makes `#rubrics/…` imports follow the folder automatically). Symbols: `RubricType` → `CriterionKind`, `Rubric` → `Criterion`, `AssessedRubric` → `AssessedCriterion`, `AssessmentRubricValue` → `AssessmentCriterionValue`, `rubric.ts` → `criterion.ts` (`markRubric` → `markCriterion`; kind-specific `markBooleanRubric` → `markCheckCriterion` etc. — functions **keep** the noun, paralleling generic `markCriterion`), leaf `rubricId` → `criterionId`. Kind-specific **components drop the leaf noun** (matching the existing `*GradeControl` shape): `BooleanGradeControl` → `CheckGradeControl`, `OrdinalGradeControl` → `OptionsGradeControl`, `NumericalGradeControl` → `NumberGradeControl`; `BooleanRubricEditorPaper` → `CheckEditorPaper`, `OrdinalRubricEditorPaper` → `OptionsEditorPaper`, `NumericalRubricEditorPaper` → `NumberEditorPaper`. `RubricCriterion.tsx` → **`CriterionGradeRow`** (restores the role suffix its pre-Mantine ancestor `RubricGradeRow` carried; *not* bare `Criterion`, which collides with the `Criterion` type and breaks the near-universal `<Noun><Role>` component convention). YAML inner `rubrics:` → `criteria:`, `type:` → `kind:`, enum values renamed. **Combined leaf+assess identifiers move only their leaf noun now, keeping "Assessment"** (the assess→grade rename is stage 5): `rubric_assessment` → `criterion_assessment`, `boolean_rubric_assessment` → `check_criterion_assessment` (+ ordinal/numerical), `RubricAssessment` → `CriterionAssessment`, FK `rubricAssessmentId` → `criterionAssessmentId`. This second touch (→ `…Grade` in stage 5) is unavoidable: leaving the leaf noun as `rubric` here would collide `criterion_assessment.rubricId` with the container's new `rubricId` through all of 2b. **Not touched in stage 2 (stage 5, assess axis):** `AssessmentStatus.tsx`, `saveAssessment`, the `assessment-*` modules, and the `onAssess`/`assessmentStatus`/`assessment` props on the renamed leaf components.
   - **2b — container `Question` → `Rubric`. One atomic PR** (unlike 2a's leaf-first split): 2b has no `rubricId` reuse hazard to sequence around (the leaf already vacated `rubricId`; `results/` pre-adopted the container `rubricId`), and the table rename regenerates the Kysely types, so schema + type regen + all code identifiers must land together to compile. Routes/YAML/UI ride along rather than splitting into a half-renamed tree. Only after 2a fully lands (old `src/rubrics/` gone, so `src/questions/` can take the name). Folder `src/questions/` → `src/rubrics/`, `src/question-management/` → `src/rubric-management/`; `questionId` → `rubricId`, `Question` (type) → `Rubric`, container symbols `QuestionTable` → `RubricTable`, `QuestionForm` → `RubricForm`, `DeleteQuestionDialog` → `DeleteRubricDialog`, `SelectedQuestionPane` → `SelectedRubricPane`, `QuestionsManagementClient` → `RubricsManagementClient`, `useQuestionDraft` → `useRubricDraft`; the **definition layer** lands on CONTEXT's canonical **Rubric Definition** term (`RubricDefinition` is free — the leaf uses `CriterionDefinition`): `QuestionDefinition` → `RubricDefinition` (and its `see CONTEXT.md: "Question Definition"` comment → `"Rubric Definition"`), `QuestionDefinitionInput`/`QuestionDefinitionPayload`/`questionDefinitionSchema`/`parseQuestionDefinitionPayload`/`questionDefinitionCacheTags` → `Rubric…` equivalents, `QuestionEditorValue`/`createEmptyQuestionEditorValue` → `RubricEditorValue`/`createEmptyRubricEditorValue`, files `questionDefinitions.ts`/`questionDefinitionMutations.ts` → `rubricDefinitions.ts`/`rubricDefinitionMutations.ts`. On these same types, `minScore`/`maxScore` (score→value, stage 7b) and `assessmentCount` (assess→grade, stage 5) stay untouched — not 2b's axis; YAML top-level `questions:` → `rubrics:`; route param `[questionId]` → `[rubricId]` and folders `assessments/submissions/[submissionId]/questions/[questionId]/` → `…/rubrics/[rubricId]/`, plus `questions/`, `export/questions/`, `import/questions/` → `rubrics/…`. The `src/projects/projectPaths.ts` **path helpers** that build these URLs move in lockstep (only their `Question` axis; the `project` prefix waits for stage 6): `projectQuestionsPath` → `projectRubricsPath`, `projectAssessmentSubmissionQuestionPath` → `projectAssessmentSubmissionRubricPath`, `projectImportQuestionsPath` → `projectImportRubricsPath`, `projectExportQuestionsPath` → `projectExportRubricsPath`, type `ProjectSubmissionQuestionRef` → `ProjectSubmissionRubricRef` (`questionId` → `rubricId`), and each helper's `/questions` URL segment → `/rubrics`. Verified every internal `/questions` link flows through these helpers (no hardcoded URL strings bypass them), so internal navigation moves atomically — only externally-bookmarked URLs break, which hard cutover accepts. **Un-mechanical:** the `Grid` type (`{ [id: string]: Question }`, used as `questionGrid`/`toQuestionGrid`) must **not** become `RubricGrid` ("rubric grid" is glossary-forbidden — CONTEXT `Rubric`) nor stay `Grid` (collides with stage 6's container concept) — rename to **`RubricsById`** (`questionGrid` → `rubricsById`, `toQuestionGrid` → `toRubricsById`), freeing `Grid` for stage 6.
     - **Scope — `questionId` is fully vacated here, including the assessment layer (decided while grilling 2b).** The container FK `questionId` → `rubricId` moves on `criterion.questionId` **and** `assessment.questionId`, and across every `questionId` reference in the `assessment-*` / `imports/assessments` modules (~27 files) plus the cache tags (`questionListCacheTag`'s `"questions"` value, `assessments:${submissionId}:${questionId}`, `assessments:question:${questionId}`). This is the same accepted "second touch" as 2a's leaf identifiers: these files move their **container noun** now (`questionId` → `rubricId`) and their **word** later (`assessment` → `grade`, stage 5). No collision — `results/` already pre-adopted the container `rubricId`. Rationale: after 2b, `question`/`questionId` is gone from the tree entirely and stage 5 stays a clean single-axis assess→grade rename. The `assessment` **word** itself (tables `assessment`/`criterion_assessment`, `saveAssessment`, `assessment-*` module/prop names) does **not** move here — stage 5.
     - **Fold in a stage-2a `type`→`kind` miss (decided while grilling 2b).** 2a claimed "one vocabulary end to end" for the criterion-*kind* axis but left a cluster still spelled "type", all describing a `CriterionKind` and all inside files 2b renames anyway (`imports/questions/` → `imports/rubrics/`, `question-management/` → `rubric-management/`): type `QuestionImportCriterionTypeChange` → `RubricImportCriterionKindChange`, `criterionTypeChanges` → `criterionKindChanges`, `fromType`/`toType` → `fromKind`/`toKind`, diagnostic string `"criterion-type-change-blocked"` → `"criterion-kind-change-blocked"`, `typeChangedCriterionCount` → `kindChangedCriterionCount`, `typeChangeNote` → `kindChangeNote`, `isTypeChanged` → `isKindChanged`, plus the **user-facing** import message "criterion type(s) were changed" → "criterion **kind**(s) were changed" (the standing lexicon violation — Kind entry lists `type` under _Avoid_). Marginal cost ~nil since these exact lines move for the container rename regardless; flag in the PR as completing the 2a miss.
   - **External contracts — hard cutover** (CONTEXT Cutover Strategy): no dual-accept YAML, no YAML version field, no URL redirects. Old-format YAML is **rejected loudly** (unknown top-level `questions:` key → blocking diagnostic), never a silent no-op import. **Mechanism (decided while grilling 2b):** the renamed top-level schema (`questions:` → `rubrics:`) gets `.strict()` so a stale `questions:` key errors with a *named* `Unrecognized key: "questions"`, not just a generic `rubrics: Required`. Today's `questionsSchema` is a plain `z.object` that silently strips unknown keys, so this `.strict()` is a real (small) behavioral add beyond the mechanical rename — without it the rejection is loud (required key missing) but doesn't name the culprit. The ZodError already surfaces via `toImportErrorState` (`questionsImportAction`), so no new error plumbing is needed. The assessment **CSV** `q1:r1` format is unchanged on-wire (colon-separated public ids stay); only code identifiers and the `AssessmentsImportForm` help-text label `questionId:rubricId` → `rubricId:criterionId` move.
3. **Team → Group** — schema, code, students CSV, UI. **One atomic PR** (schema rename + Kysely regen + all code must compile together; no leaf/container reuse hazard — nothing else claims `group`/`group_id`). (Grilled 2026-07-10.)
   - **Migration.** Rename table `team` → `group` (**`group` is a SQL reserved word — raw SQL must quote `"group"`; the Kysely schema/query builder auto-quotes**), join table `student_to_team` → `student_to_group` (`team_id` → `group_id`), `submission.team_id` → `group_id`, and the enum **value** `submission_type` `'team'` → `'group'` via `ALTER TYPE "submission_type" RENAME VALUE 'team' TO 'group'`. Constraints/indexes are all snake_cased after the 2b follow-up, so plain deterministic `renameConstraint`/`renameColumn`/`renameTo`: `team_name_project_id_key`/`team_project_id_fkey` → `group_*`; `student_to_team_student_id_fkey` → `student_to_group_student_id_fkey`, `student_to_team_student_id_team_id_pkey` → `student_to_group_student_id_group_id_pkey`, `student_to_team_team_id_fkey` → `student_to_group_group_id_fkey`, index `student_to_team_team_id_index` → `student_to_group_group_id_index`; `submission_team_id_fkey` → `submission_group_id_fkey`, unique index `submission_team_id_key` → `submission_group_id_key`. **No trigger work** (all triggers are criterion-scoped; neither `team`, `submission`, `student`, nor `student_to_team` carries one). **CHECK `submission_type_participant_check` is left untouched** — Postgres relabels the enum value in place and auto-updates dependent constraint/column references on `RENAME VALUE` + `RENAME COLUMN`, so its body re-derives `'group'`/`group_id` with no drop/recreate; its name keeps "participant"/"submission" (stage 4's `submission_*` sweep). Verify the body via an up/down/up round-trip on a PG17 testcontainer. DDL one statement at a time (no `Promise.all` — concurrent `ALTER` only contends for locks).
   - **Axis split (second touch).** Stage 3 moves only the **group noun**. The enum **type name** `submission_type` / TS `SubmissionType` and the DB column `type` → `kind` are **stage 4** (bundled with `submitter` → `name` and the rest of the grades-CSV reshape) — pulling them forward would be an isolated slice of stage 4 that re-touches `submission`-adjacent files. (Flag for stage 4: the enum type can't become bare `kind` — collides with `criterion_kind`; pick e.g. `grade_target_kind`. The `kind` in "grades CSV `kind` column" is the column header, not the enum type name.) The **submission** word stays everywhere (`submission`/`Submission` table+types, `submissionId`, `SubmissionType`, `type` field, `SubmissionSelector`, `getSubmissionLabel`, `submissionExportGrouping`, `submissionsByOwner`); only their inner `team`/`teamName` refs move. Regen yields `SubmissionType = "individual" | "group"`; every `type: "team"` / `=== "team"` literal flips to `"group"` (forced by the value rename, not optional).
   - **Code sweep.** Every `team`/`Team` token → `group`/`Group`: locals (`teamsByName`, `teamNames`, `isTeam`, `currentTeamName`, `teamMemberQuery`), fields (`teamName` → `groupName`), DB keys (`studentToTeam` → `studentToGroup`, `teamId` → `groupId`), string literals, thrown error messages ("…mapped team…" → "…mapped group…"), and comments. Mixed-noun identifiers move only their group half (`teamMembersBySubmissionId` → `groupMembersBySubmissionId`, keeping `submission`). Files: `submissions/{types,submissions,getSubmissionLabel}.ts`, `imports/students/{saveStudents,parseStudents,studentImportContext,prepareStudentImport,StudentsImportForm}.*`, `imports/{schemas,constants,types}.ts`, `imports/assessments/assessmentImportContext.ts`, `export/{submissionExport,submissionExportGrouping,submissionExportCsv}.ts`, `assessment-capture/{quickJumpSearch,SubmissionSelector}.*`, plus their tests. **No ALL-CAPS `TEAM` constant exists** (grep = 0) — nothing to special-case.
   - **UI copy — team→group only (single-axis).** `SubmissionSelector`: "team or student name" → "group or student name" (placeholder + description). `StudentsImportForm`: "student or team data" → "student or group data", CSV column mention `team` → `group`, "students sharing a team" → "students sharing a group". **Leave the "submission" leak** ("…grouped into the same submission") **in place and flag it for the stage-7 UI-copy audit** — the submission word is stage 4/7's axis, not stage 3's; matches the accepted-half-state discipline.
   - **CSV / e2e.** *Required (contract):* students header `team` → `group` in `STUDENTS_CSV_PLACEHOLDER`, `e2e/fixtures/students.csv`, and `schemas.ts` (the `team` field + `"team" in row` guard); assessments `submission_type` **value** `team` → `group` (header `submission_type` stays — stage 4) in `ASSESSMENTS_CSV_PLACEHOLDER`, `e2e/fixtures/assessments.csv`, and `schemas.ts` `z.enum(["individual","team"])` → `["individual","group"]`. *Cosmetic (free-text group name, not a keyword):* sample group `Team A` → `Group A` across the placeholders, fixtures, and `e2e/fixtures/expectations.ts` (`label`, keys, and the "team"→"group" wording in its comments) — nothing breaks if left, but a fixture group literally named "Team A" right after retiring "team" is confusing. Leave the placeholder's `q1:r1` sample keys (public-id samples, unrelated to team).
   - **Docs.** Add to `docs/reference/database-migrations.md`: (1) the `group` table is a reserved word → quote `"group"` in any raw SQL (builder auto-quotes); (2) `RENAME VALUE`/`RENAME COLUMN` auto-update CHECK-constraint bodies, so an enum-value/column rename needs no constraint drop/recreate. **No ADR** (localized, cheap to revisit). **No CONTEXT.md/lexicon change** — **Group** ("Previously called 'Team'") and **Grade Target** are already correct; stage 3 surfaces no new terminology, only applies settled vocab.
   - **Validation.** Full gate incl. `test:e2e` — this is the first e2e run since 2a, so it also exercises 2b's changes (2b's handoff notes its e2e/manual pass wasn't run) — plus a manual browser pass.
4. **Submission → Grade Target** — the #136 core. **One atomic PR, one migration** (rekey + rename together, following the `20260518000001_rekey_question_and_rubric` atomic-FK/PK-transition-with-backfill precedent; nothing is named `grade_target`/`gradeTargetRowId`/`grades`-route today and `grade` does not arrive until stage 5, so no 2a/2b-style reuse split is forced). Schema (`submission` → `grade_target`), routes to their final shape, cache tags, exports, and the grades-CSV reshape. **No new CONTEXT/lexicon term and no ADR** — Grade Target, Name, Kind, and the `id`/`row_id` convention (#51) are all already settled; stage 4 only applies them (same posture as stage 3). (Grilled 2026-07-10.) **Issue tracking — do NOT close #136 here.** Stage 4 does the bulk of #136 (submission→Grade-Target identity: public `id` + `row_id`, the target-identity FK `_row_id` renames, routes/exports/cache, resolve-by-`projectId+targetId`), but one #136 requirement remains — bullet 5's "rename touched FK columns that target internal row ids" is not complete while `grade_target.project_id` still targets `project.row_id` under `_id` naming. That FK renames across every table together in **stage 6** (Project→Grid), so **stage 4's PR uses `Related to #136`** (not `Closes`), and **stage 6's PR carries `Closes #136`**. Distinct from this: Group's own `id`→`row_id` normalization is **out of #136 scope entirely** (not a #136 requirement — Group has no public-id need), so it is not "remaining #136 work" and is not a reason #136 stays open.
   - **Public Grade Target ID (the #136 core) — `t-<n>`: a string id carrying a per-grid sequential suffix** (format settled while grilling 2026-07-10, after two rejected iterations: opaque `t-`+nanoid, then bare integer). The generated numeric `submission.id` becomes internal `row_id`; the new **public `id`** is **`text`** — `t-` + a per-grid creation-order integer (`t-1`, `t-2`, …), **generated, never derived** from student id / group name / display label. String-and-prefixed so it is unmistakably an *id* (uniform with every other entity's string `id`; no bare integer confusable with `row_id`); sequential rather than opaque because opacity buys nothing here — access to a grid already exposes all its targets, so within-grid enumeration leaks nothing new (contrast Project's `p-`+nanoid, opaque because top-level: no parent scope gates enumeration there). Uniqueness is **grid-scoped** — unique index on `(project_id, id)` — and every app-facing load/save **resolves targets by `projectId + targetId`, never `targetId` alone** (CONTEXT Grid Resolution Strategy).
     - **Generation — parse-and-max in SQL, no counter state.** At creation, compute the next suffix **inside the import transaction**: `SELECT COALESCE(MAX(substring(id FROM 3)::int), 0) + 1 FROM grade_target WHERE project_id = ?`, then assign ascending `t-<n>` ids to the batch. The DB does the parsing and nothing is fetched — a grid holds a class (tens of rows), so the tiny per-grid scan needs no expression index. **Reuse after deletion is accepted** (decided while grilling: a suffix may be re-issued if the top target is ever deleted — deletion doesn't exist today, #45), so no high-water counter column on the grid. The `UNIQUE (project_id, id)` index backstops a concurrent-import race: two imports reading the same MAX collide on insert and one transaction fails, which the all-or-nothing import policy already handles. **Never a global sequence** — that leaks the *global* target count, the exact enumeration leak grid-opacity prevents. Import (`saveStudents.ts`) is the only creation path today.
     - **Backfill (in this migration).** Assign existing rows `'t-' || ROW_NUMBER() OVER (PARTITION BY project_id ORDER BY row_id)` (row_id ≈ creation order).
     - **App-facing type is `string`** (`GradeTarget.id: string`), uniform with every other entity — no parsing at the route boundary. **Ordering landmine:** a text column does not sort its suffix numerically (`t-9` > `t-12` lexicographically), so anywhere targets must list in creation order, `ORDER BY row_id` (creation order by construction) — never `ORDER BY id`. Today's loaders (`loadSubmissionsFromDb`, export) order by the numeric `submission.id`, which *becomes* `row_id`, so the mechanical rename lands them on the correct column automatically — but any new query must remember the rule.
   - **`id`/`row_id` + FK `_row_id` renames.** Rename numeric `id` → `row_id` first, then add the text `id` (the two `id` columns cannot coexist). Rename the three **target-identity** FK columns to the `_row_id` convention: `grade_target.student_id` → `student_row_id` (targets `student.row_id`), `grade_target.group_id` → `group_row_id` (targets `group.id` — honest: it *is* the internal surrogate, even though Group still names its own key `id`), and `assessment.submission_id` → `grade_target_row_id` (**not** bare `target_row_id` — CONTEXT avoids bare "target"). **Leave `grade_target.project_id`** unchanged — the project/grid axis (`project_id` → `grid_row_id`) is stage 6, done across every table at once. **Do not normalize Group's own `id` → `row_id`** or give Group a public id — out of #136 scope, Group has no public-facing id need (referenced only internally); flag as a future group-normalization gap.
   - **Kind axis.** Enum type `submission_type` → **`grade_target_kind`** (cannot be bare `kind` — collides with `criterion_kind`; matches the `<entity>_kind` shape, and the values *are* the kind of target), TS `SubmissionType` → `GradeTargetKind`, column `type` → `kind`; values stay `individual`/`group`.
   - **Routes — full final shape** (retire the `assessments/` URL segment + `projectAssessments*` helper names now; stage 5 keeps only the assess→grade **word** work on tables/modules/props). `/assessments` (grading hub) → `/grades`, `/assessments/submissions/[submissionId]` → `/grades/[targetId]/[targetSlug]`, `/assessments/submissions/[submissionId]/rubrics/[rubricId]` → `/grades/[targetId]/[targetSlug]/rubrics/[rubricId]`, `/assessments/results` → `/results` (grid root). **Include the cosmetic `[targetSlug]` now** — reuse `CosmeticSlugReplacement`; add `toTargetSlug(label)` mirroring `toProjectSlug` (a target's display label is validated non-empty — student `first_name`/`last_name` are `nonEmptyString`, and an empty group name is coerced to no-group/individual — so no empty-label branch is needed; the loader's leftJoin `?? String(id)` fallbacks are can't-happen guards). **Fix `canonicalizeSlugSegment`'s addressing while adding the second id/slug pair.** The URL structure is unambiguous by position (Next.js binds `[targetId]` positionally; no URL revisiting needed), but the helper infers the slug's position by *searching the path for the id's value* (`segments.indexOf(id)`) — backwards, and only ever safe because its sole caller uses globally-unique `p-…` values (a grid named "T 3" slugs to exactly `t-3` and would be matched first). Rewrite it to **positional addressing**: the layout that owns `[targetId]/[targetSlug]` passes the id's segment position, the helper replaces `segments[position + 1]`, keeping the id as a *verification* (`segments[position] === id`, throw on mismatch — preserves the existing misuse guard). Value collisions then cannot matter, structurally. Update the project caller to the same API and cover the collision case in the test. Path helpers move off "assessments": `projectAssessmentsPath` → `projectGradesPath`, `projectAssessmentSubmissionPath` → `projectGradeTargetPath` (takes `targetId` + `targetSlug`), `projectAssessmentSubmissionRubricPath` → `projectGradeTargetRubricPath`, and `projectResultsPath` drops its `/assessments` segment. Hard cutover (no redirects) — 2b verified internal links flow through these helpers. `[targetId]` is the **only** user-facing appearance of the "target" word (URLs/UI otherwise name the Student or Group).
   - **Grades-CSV — complete reshape in one cutover** (settled Total vocab; the aggregate is already computed in export): header `submission_type` → `kind`, `submitter` → `name`, `grand_total_marks` → `final_total`, and the per-rubric total header (currently a bare `rubric.id`) → `rubricId:total`. The **public target id is *not* a CSV column** — the CSV keeps matching targets by the human-readable `name` (student id / group name via `getSubmissionExportIdentifier`), consistent with #136 (name for matching/display, target id for routes/APIs). Update `ASSESSMENTS_CSV_PLACEHOLDER`, `e2e/fixtures/assessments.csv`, the export/import code, and `schemas.ts` (assessment-row enum column header `submission_type` → `kind`).
   - **Cache tags — move the grade-target noun now, keep the assess word for stage 5.** `submissionListCacheTag` → `gradeTargetListCacheTag` (value `"submissions"` → e.g. `"grade-targets"`); the `assessmentForSubmission*` tags move only their submission→target noun (`assessmentForSubmissionCacheTag` → `assessmentForGradeTargetCacheTag`, value `assessments:${targetId}`) while keeping the `assessment` word — the same accepted second-touch as 2b.
   - **Migration landmines.** Beyond the rename, this migration also **adds + backfills the public `id text`** (`'t-' || ROW_NUMBER()` per grid), then adds `UNIQUE (project_id, id)` and `NOT NULL` — sequence these after the `id`→`row_id` rename, since the two `id` columns cannot coexist. Post-snake_case names are deterministic, so use plain `renameConstraint`/`renameColumn`/`renameTo`/`alterType(...).renameTo(...)`. But **the default PK `submission_pkey` and the per-table index `submission_project_id_idx` do not auto-rename with the table** (the exact `team_pkey`/`team_project_id_idx` and `question_pkey`/`question_project_id_idx` landmine from stages 3 and 2b — found only by inspecting the live schema, not static review) — rename them explicitly to `grade_target_pkey`/`grade_target_project_id_idx`. Full rename set: `submission_project_id_fkey` → `grade_target_project_id_fkey`, `submission_student_id_fkey` → `grade_target_student_row_id_fkey`, `submission_group_id_fkey` → `grade_target_group_row_id_fkey`, unique indexes `submission_student_id_key`/`submission_group_id_key` → `grade_target_student_row_id_key`/`grade_target_group_row_id_key`, CHECK `submission_type_participant_check` → `grade_target_kind_participant_check`, and on `assessment`: `assessment_submission_id_fkey` → `assessment_grade_target_row_id_fkey`, `assessment_submission_id_rubric_id_key` → `assessment_grade_target_row_id_rubric_id_key`. The CHECK body auto-re-derives on the `type`→`kind` column rename and the enum type-name rename (OID/attnum-tracked, per `docs/reference/database-migrations.md`); **verify empirically** (`\d+ grade_target` + a CHECK-violation test) after an up/down/up round-trip on a PG17 testcontainer, as stage 3 did. DDL one statement at a time (no `Promise.all`).
   - **Validation.** Full gate incl. `test:e2e` + a manual browser pass.
4a. **Adopt kanel + kanel-kysely as the type generator** — tooling-only PR, no schema change (decided while grilling stage 5, 2026-07-12; supersedes an interim "regex-suffix the kysely-codegen output" idea). Motivation: the generated PascalCase table interfaces collide with same-named domain types (`Criterion`, `CriterionKind`, `GradeTarget` are live collisions today), and stage 5 wants bare **`CriterionGrade`** for the domain payload. kanel generates `XTable`-suffixed interfaces natively (Kysely's own documented idiom) and its hooks solve the rest as supported configuration. Also front-loads #132's toolchain: kanel's `generateIdentifierType` is the natural branded-id mechanism, deliberately **not enabled here** — branding waits until after stage 6 so brand names (`GridRowId`, `CriterionGradeId`, …) are born final (renames first, brands after). Shape: `.kanelrc` with `makeKyselyHook` + `kyselyCamelCaseHook` (query clients keep `CamelCasePlugin`), `typeFilter` excluding kysely migration tables, `outputPath src/db/generated/` + `preDeleteOutputFolder` (folder stays generated-only), a small custom `preRenderHook` renaming the bare Selectable aliases kanel emits (`Criterion` → `CriterionRow` or drop them — they'd re-mint the collision from the generated side), **no** `generateIdentifierType`. `db:types:generate` swaps to the kanel CLI; kysely-codegen removed; the ~dozen `#db/generated/db.ts` import sites (the `DB` map + the `Project`/`GradeTargetKind` named imports) update mechanically. Gate: `check --fix`, `check-types` + full `vitest` and production `build` (regenerated types must compile against all existing queries unchanged); `test:e2e` optional here since 4a changes no runtime behavior, but 4b/5 run it regardless.

4b. **Drop the `assessment` container table** — preliminary structural PR before stage 5 (decided while grilling stage 5, 2026-07-12; see `docs/investigations/2026-07-12-assessment-container-table.md`). The (grade target × rubric) container is a day-one fossil (it backed the original `app/[questionId]/[paperId]` page structure): no reader reads anything from it, only `criterion_assessment` references it, and stage 5 cannot name it — it is neither a grade nor a total. Rekey `criterion_assessment` on `UNIQUE (grade_target_row_id, criterion_id)` (backfill `grade_target_row_id` from the parent, drop `assessment_id`, drop the table), rewrite the seven query sites (save path, capture load, results, completion, import context, delete-impact count, export), and update `CONTEXT.md`'s **Grade** entry ("the rubric-level grouping record in persistence is a container…") once the container no longer exists. Delete-impact count semantics change from "containers" to "distinct targets with real grades" — more truthful, no UI copy change. Down migration recreates containers from distinct `(grade_target_row_id, criterion.rubric_id)` pairs (empty containers unrecoverable — they are the bug state). **Two riders folded in (decided while grilling):** (1) **fix the partial-commit bug** — the rewritten save path validates the subtype payload *before* the first write, so a failed first-time save no longer commits an empty kind-only grade that completion miscounts (the drop PR rewrites this path anyway; it's a statement-order choice); (2) **drop the redundant `criterion_assessment.kind` column** — it always equals the immutable, always-joined `criterion.kind`; no trigger reads it (the `*_kind_match` triggers are on the criterion subtype tables), only `loadResults.ts` and `assessment-capture/assessments.ts` select it and both already join `criterion`, so they switch to `criterion.kind`; the save-time guard compares the incoming payload's kind and is untouched. Export ordering: drop `ORDER BY assessment.id` (confirmed unused — `groupGradeTargetRows` only needs per-target contiguity by `gradeTarget.rowId`).

5. **Assessment → Grade** — schema (`criterion_assessment` + subtype tables — the container is gone per stage 4b), `saveAssessment` and the whole `assessment-*` module set, cache tags, UI, Grade Completion. Settled while grilling (2026-07-12):
   - **Tables:** `criterion_assessment` → **`criterion_grade`** (subtypes `check_/options_/number_criterion_grade`, FK `criterionAssessmentId` → `criterionGradeId`; generated interface `CriterionAssessmentTable` → `CriterionGradeTable` per stage 4a's suffix) — not bare `grade`, which would sit confusably beside the saturated `GradeTarget`/`GradeMatrix`/`/grades` identifier family and break the `criterion`/`check_criterion` self-documenting parallel.
   - **Domain payload:** `AssessmentCriterionValue` → bare **`CriterionGrade`** (base `CriterionGradeBase`, helper `AssessmentForCriterionKind` → `CriterionGradeForKind`) — it *is* CONTEXT's Grade ("the recorded evaluation of a Criterion"), and stage 4a frees the name from the generated namespace. Not `…Value` (CONTEXT reserves **Value** for the Number payload; a union spanning check/options/number named `Value` is the violation the sweep exists to fix). Rider: `criteria/types.ts` imports the generated `CriterionKind` instead of redeclaring it (Schema Alignment Rule, matching `grade-targets/types.ts`). Footnote: the module-local joined-row type in capture (`CriterionAssessmentRow`) must not become `CriterionGradeRow` (taken by the 2a component); use a loader-local name like `criterionGradeQueryRow`. The `number` variant keeps its `score` field — score→value is stage 7b.
   - **Save chain:** `saveAssessmentInDb` → `saveCriterionGradeInDb` (DB Primitive `…InDb`), app wrapper + server action → bare **`saveCriterionGrade`** (App-Level Wrapper convention; action keeps sharing the wrapper's name as today), client no-throw helper `saveCriterionAssessment` → **`trySaveCriterionGrade`** (`try` = returns outcome, never throws — the `tryDeserialize` precedent; **not** `submit*`, a verb the repo doesn't use and the root of the retired Submission word). `SaveAssessmentParams`/`Result` → `SaveCriterionGradeParams`/`Result` (payload field `assessment:` → `grade: CriterionGrade`), injected type `SaveAssessment` → `SaveCriterionGrade`, `assessmentErrors` → `saveCriterionGradeErrors`.
   - **Modules:** `assessment-capture` → **`grading`** (the interactive grading vertical; "capture" only existed to pair with the noun "assessment"), `assessment-completion` → `grade-completion`, `assessment-persistence` → `grade-persistence`, `imports/assessments` → `imports/grades`; route `import/assessments` → `import/grades`.
   - **Components/hooks:** page clients named by the page they drive — `GradeTargetAssessmentClient` → **`RubricGradingClient`** (single-rubric page), `GradeTargetOverviewAssessmentClient` → **`GradeTargetGradingClient`** (target's all-rubrics page; also removes a stray "Overview" before stage 8 makes that word mean grid-home). Summary strips take short forms since their modules already carry the word: `AssessmentProgressSummary` → **`CompletionSummary`** (grading), `GlobalAssessmentSummary` → **`GlobalCompletionSummary`** (grade-completion). Mechanical same-axis set: `useAssessmentSession` → `useGradingSession`; `AssessmentStatus` → `GradeStatus` (+ module.css), prop values `"unassessed"/"assessed"` → `"ungraded"/"graded"`; `AssessedCriterion` → `GradedCriterion`; `assessedCount` → `gradedCount`; `loadAssessedCriterionKeys` → `loadGradedCriterionKeys`; `assessmentCompletion.ts`/`loadAssessmentCompletion.ts` → `gradeCompletion.ts`/`loadGradeCompletion.ts`; `onAssess`-family props → `onGrade`; cache tags `assessment*CacheTag` → `grade*CacheTag`, string values `assessments…` → `grades…` (+ `docs/reference/cache-invalidation-map.md`; stage 6's grid-scoping caveats stay).
   - **progress → Completion folded in** (was floating between stages): stage 5 owns the completion module, so the settled progress→Completion rename (`progress`/`progressPromise`/`progressByTargetId` data names, `AssessmentProgressSummary` → a Grade-Completion-worded name, `CompletionProgress`'s `assessedCount` prop) rides here rather than manufacturing transient second-touch names for stage 7. Carve-out: Mantine's `<Progress>` primitive and honest progress-*bar* widget naming stay — the rename targets the domain concept (progress-as-completion-data), not every literal token.
   - **User-facing copy + e2e (rides stage 5 — assessment IS this stage's axis, per the stage-3 single-axis precedent):** grid-home tiles "Students and groups **assessed**"/"Rubrics assessed"/"Criteria assessed" → "…**graded**" (lexicon **Completion** avoids "assessed"); import form "Assessments CSV" → **"Grades CSV"**, "Import assessments" → **"Import grades"** (matches route `import/assessments` → `import/grades`); import errors "Assessment import errors:"/"No assessment columns found…" → "Grade import errors:"/"No grade columns found…". e2e (mandatory per sweep-scope rule): update the completion-tile matchers, the import navigation/labels, rename fixture `e2e/fixtures/assessments.csv` → `grades.csv`, and the `expectations.ts` comments.
   - **Mechanical file/symbol sweep (same axis):** `AssessmentsImportForm` → `GradesImportForm`, `prepareAssessmentImport`/`saveAssessments`/`assessmentImportContext` → `prepareGradeImport`/`saveGrades`/`gradeImportContext`, `ResultsAssessmentRecord`/`loadCriterionAssessmentRecordsFromDb` → `ResultsGradeRecord`/`loadCriterionGradeRecordsFromDb`, `AssessmentImportPlan`/diagnostics → `Grade…`, etc. One accuracy nudge folded in: rubric-management's `assessmentCount` → **`gradedTargetCount`** (post-4b it counts targets with real grades — the name should say so; delete-impact copy unchanged).
   - **CONTEXT.md updates (ride this stage's implementation):** drop the now-false "…is a container, not a second kind of grade" clause from **Grade** (4b removed the container), and update the *Flagged Ambiguities* notes that cite `saveAssessment`/`RubricAssessment`/`AssessedRubric` as "tracked for a follow-up code refactor" — stage 5 makes them done.
   - **Score stays (stage 7b carve-out):** the `number` payload's `score` field, `numberCriterion.minScore/maxScore`, and the `invalidScore`/`invalidScoreRange`/"Enter a valid score" messages in the save path are the score→value axis — untouched here.
   - **PR structure:** one atomic PR + one migration (rename `criterion_assessment` + the three `*_criterion_assessment` subtypes → `*_criterion_grade`, FK `criterionAssessmentId` → `criterionGradeId`, and the two grade-subtype triggers that sit *on* those tables — `trg_ordinal_label_valid`, `trg_numerical_score_bounds` — plus their constraint/index renames). No leaf/container reuse hazard (4b removed the container; `grade` collides with nothing), so no 2a/2b-style split. Types regenerate through kanel (stage 4a). Full gate incl. `test:e2e` + a manual browser pass.
6. **Project → Grid** — schema (`project` → `grid`, already on `id`/`row_id`), all FKs, routes (`/projects` → `/grids`), cache tags, code, UI. Done last: it touches the most files and is the outermost identifier, so doing it after the inner renames avoids re-touching. **Closes #136** — this stage completes #136's last outstanding requirement (bullet 5, "rename touched FK columns that target internal row ids"): every `project_id` FK, including `grade_target.project_id`, becomes `grid_row_id` here. Put `Closes #136` in this PR's "Related issue" section (stage 4 only used `Related to #136`). **Also grid-scope the id-keyed assessment cache tags** while threading grid identity through: `assessments:{target}`, `assessments:{target}:{rubric}`, and `assessments:rubric:{rubric}` key on ids that are only unique within a grid, so same-numbered ids in two grids share a tag today (over-invalidation only, never staleness — flagged in the stage-4 review; caveat recorded in `src/db/cacheTags.ts` and `docs/reference/cache-invalidation-map.md`). Prefix them with the grid id here and drop both caveat notes.
7a. **App name** — replace the stale `"BonPoint"` title fallbacks (`src/app-shell/AppShellTopBar.tsx`, `AppShellLoadingShell.tsx`) and the `app/layout.tsx` metadata description ("Simple assessment helper for rubric-based evaluation") with **Tardigrade** copy. Public-facing name only; repository, package, and technical identifiers stay `grading` (#106).

7b. **Score → Value everywhere** — "score" is fully eliminated, not kept internal: it becomes **value** across DB (`numerical_criterion.min_score`/`max_score` → `min_value`/`max_value`, `numerical_criterion_assessment.score` → `value`), code, YAML, and UI (editor `Min score`/`Max score` → `Min value`/`Max value`; grade placeholder `"Score"` → `"Value"`; `"Enter a valid score"` etc. → value wording; the reversed flag's "score-to-marks mapping" → "value-to-marks"). Where bare `value` is ambiguous in code, use `criterionValue`. One vocabulary, no internal/external split — same rule as the criterion-kind rename. Chosen over "score" because a Number criterion can be reversed (higher input → fewer marks), where "score" is semantically wrong but "value" stays correct.

7c. **Check answers → Yes/No, polarity-neutral** — the two answers are user-facing **Yes**/**No** (editor `True marks`/`False marks` → `Yes marks`/`No marks`; grade-control segment labels True/False → Yes/No). The internal boolean (`passed`, `marks`/`falseMarks`) may stay, since the underlying type is genuinely boolean. Also de-polarize the grade control: the green-✓/red-✗ coloring asserts true=good, wrong for a reversed Check (more marks on No) — color neutrally or by marks earned, not by the answer.

8. **Dashboard → Overview (grid home)** — retire "Dashboard" as user-facing vocabulary: the grid-home page (`app/projects/[projectId]/[projectSlug]/page.tsx`) title `{name} Dashboard` → Overview, breadcrumb/home nav, and `projectDashboardPath` → `projectOverviewPath`. Split out of stage 1 because it lives in a different surface (grid home / `assessment-completion` / `app-shell`, not the analytics module). **Must run after stage 1**, which frees the `projectOverviewPath` name by renaming the readout page's path to `projectResultsPath`. Cosmetically independent of stage 1 (the home page already shows status via `GlobalAssessmentSummary`); will be re-touched by Project→Grid (stage 6) regardless.

7. **UI copy audit and contract docs** — sweep every user-facing string against `docs/reference/lexicon.md`: "Grades"/"Analytics"/"Name" labels, error messages, empty states, headings. Add any missing Lexicon entries surfaced during the audit (the Lexicon is a dictionary only — word: definition; contracts don't live there). Document the implemented URL tree and import/export column sets durably: README Import Formats plus a reference doc for URL conventions — this plan's "Routes" and "CSV columns" sections above are the spec until then.
   - **Whole-README refresh, deferred to here.** The README is stale across every terminology axis and is *not* updated stage-by-stage (it's contributor-facing docs, not shipped code, so it stays wrong until the sweep lands and is corrected once): the tagline and feature bullets ("rubric-based assessment", "Import questions", "Grade submissions by submission or by question"), the App Workflow steps, and the entire **Import Formats** section — Questions YAML (`questions:`/`rubrics:`-as-leaf, `boolean`/`ordinal` kinds, `minScore`/`maxScore`, `reversed` "score-to-marks"), Students CSV (`team`), and Assessments CSV (`submission_type`, `submitter`, `q1:r1`, marks columns). Rewrite it whole against the final vocabulary once stages 1–8 and 7a–c have landed.

## State (handoff)

- **Stage 1 done** — [PR #245](https://github.com/QuentinRoy/tardigrade/pull/245): `src/rubric-analytics/` → `src/results/` with the full `RubricOverview*` → `Results*`/`CriterionRow`/`GradeTargetRow` symbol-family rename, the leaf/container swap (`SubmissionMatrix` → `GradeMatrix`, `RubricAnalyticsTable` → `CriterionAnalyticsTable`, tooltip swap), page retitled Results with Analytics/Grades sections, route `/assessments/overview` → `/assessments/results`, summary strip removed (mooting the original "Class average" → "Average total" bug-fix), Grades table's "Average" column relabeled "Total". Grilling this stage also settled the Overview-vs-Results split now reflected in stages 1/8 above and in the lexicon. Validated: `check --fix`, `check-types`, `lint:boundaries`, full `vitest` suite (361/361), production `build` + `test:e2e`, manual browser check against a real project — all green.

- **Stage 2a done** — leaf `Rubric` → `Criterion` with the criterion-kind rename folded in. Migration `20260707000000_rename_rubric_to_criterion.ts` renames the leaf + subtype tables straight to their final names (`boolean_rubric` → `check_criterion`, `ordinal_rubric` → `options_criterion`, `ordinal_rubric_value` → `options_criterion_mark`, `numerical_rubric` → `number_criterion`, `rubric_assessment` → `criterion_assessment` + `*_assessment` subtypes), the enum `rubric_type` → `criterion_kind` (values `boolean`/`ordinal`/`numerical` → `check`/`options`/`number`), the `type` → `kind` columns, and all six trigger functions. Folder `src/rubrics/` → `src/criteria/` (`rubric.ts` → `criterion.ts`, `RubricCriterion` → `CriterionGradeRow`, `*GradeControl` and `*EditorPaper` on the check/options/number kind words), plus the app-wide leaf-vocabulary sweep (types, DB queries, imports YAML `type`/values → `kind`/values, exports, tests). `results/` keeps its container `rubricId`/`rubricLabel` (pre-adopted 2b names) while its leaf refs move. Validated: `check`, `check-types`, `lint:boundaries`, full `vitest` (361/361, incl. integration on a PG17 testcontainer). The migration renames the enum (`alterType`), tables (`renameTo`), and columns (`renameColumn`) with the Kysely schema builder, and uses raw SQL only for constraint renames, index renames, and trigger/function management. **A subtlety worth recording:** all Kysely clients (both migration runners and both query clients) use `CamelCasePlugin`, which `snake_case`s identifiers passed to the schema builder. Earlier migrations created some constraints via the builder (stored `snake_case`d) and others via raw SQL (stored verbatim PascalCase), so a single `renameConstraint` call can't match them all — the migration instead renames each constraint by looking it up by any candidate spelling (a `DO` block over `pg_constraint`, which bypasses the plugin), leaving no stale `rubric` name. DDL is run one statement at a time (no `Promise.all`: concurrent `ALTER` only contends for locks). This and the "prefer the Kysely builder; raw SQL only when it can't express the change" rule are documented in `docs/reference/database-migrations.md`. **Two naming deviations from the pre-implementation plan, by design:** (1) `RubricGradeList` → `CriterionGradeList` (the Q4 grilling had said keep it; renamed for consistency since it renders `CriterionGradeRow` items — flagged for review). (2) kind-specific *mark functions* keep the noun (`markCheckCriterion`), unlike the noun-dropping editor/control components.

- **Stage 2b done** — container `Question` → `Rubric`, one atomic PR. Migration `20260709000000_rename_question_to_rubric.ts` renames table `question` → `rubric` and the container FK `question_id` → `rubric_id` on `criterion` and `assessment` (schema builder throughout — `renameTo`/`renameColumn`/`renameConstraint` for the 7 constraints — plus one raw `ALTER INDEX` for the lone plain index `question_project_id_idx`; **no enum/trigger work** — 2a's enforcement functions are all criterion-scoped and neither `question` nor `assessment` carries a trigger; renaming a UNIQUE/PK constraint auto-renames its backing index). up/down/up round-trip verified against a PG17 testcontainer leaving zero stale `question` names; **CamelCasePlugin removed from both migration runners** (`src/db/migrate.ts`, `createDisposableMigrationDb`) after review found the plugin retroactively snake_cased identifiers in already-committed migrations, forking constraint names between databases migrated before/after the plugin landed and breaking older `down` paths — query clients keep the plugin; migration identifiers now run verbatim, making the 2b renames deterministic without 2a's candidate-spelling `DO`-block; follow-up migration `20260709000001_snake_case_constraint_and_index_names.ts` case-folds the remaining Prisma-era PascalCase constraint/index names so stages 3–6 rename with plain deterministic `renameConstraint` calls (see `docs/reference/database-migrations.md`); Kysely types regenerated (`Question` interface → `Rubric`, `questionId` → `rubricId`). Folders `src/questions/` → `src/rubrics/` (`questions.ts` → `rubrics.ts`, `Grid` type → **`RubricsById`**, `toQuestionGrid`/`loadQuestionGrid` → `toRubricsById`/`loadRubricsById`), `src/question-management/` → `src/rubric-management/` (components `QuestionForm`/`QuestionTable`/… → `Rubric…`, definition layer → CONTEXT's `RubricDefinition`), `src/imports/questions/` → `src/imports/rubrics/`, `src/export/questionsExport.ts` → `rubricsExport.ts`, `src/test/questions.ts` → `src/test/rubrics.ts`, and the `app/**/questions/[questionId]`, `export/questions`, `import/questions`, `rubrics` route folders → `rubrics/[rubricId]`/…; `projectPaths.ts` helpers moved in lockstep (only the `Question` axis; `project` prefix waits for stage 6). Tree-wide identifier sweep (ordered special-cases → `Grid` → general `Question`/`question`), excluding committed migrations and generated types; `assessment.questionId` and every `questionId` in the `assessment-*` modules moved to `rubricId` (the accepted second touch — the `assessment` **word** waits for stage 5), and cache-tag string values (`"questions"`, `assessments:question:…`) moved too. **Decisions made while grilling 2b and applied:** (Q4) the renamed `rubricsSchema` gets `.strict()` so a stale top-level `questions:` key is rejected loudly *by name* (`Unrecognized key: "questions"`), with a new parse test; (Q5) folded a stage-2a `type`→`kind` miss (`RubricImportCriterionKindChange`, `fromKind`/`toKind`, `criterion-kind-change-blocked`, `kindChangedCriterionCount`, and the user-facing "criterion **kind**(s) were changed"). **Sweep artifacts hand-fixed:** the mechanical rename exposed a stage-2a leaf leftover in `loadResults.integration.test.ts` (local helpers `createBooleanRubric`/… actually build criteria → renamed to `createCheckCriterion`/`createOptionsCriterion`/`createNumberCriterion`, `add*Assessment` param `rubricRowId` → `criterionRowId`, colliding container/criterion row-id vars disambiguated); the glossary-forbidden phrase "rubric grid" in a test description and a mangled "multiple rubrics on the same rubric" comment were corrected, and local `grid` vars holding the by-id map renamed to `rubricsById`. **CSV/URL contracts:** the assessment CSV column key landed correctly as `rubricId:criterionId` (2a had already done the leaf half); routes are a hard cutover (no redirects). Validated: `check --fix`, `check-types`, `lint:boundaries`, full `vitest` (362/362, incl. integration on a PG17 testcontainer — up from 361 with the new old-format-rejection test), production `build` (routes render as `/rubrics`, `…/rubrics/[rubricId]`, `/export/rubrics`, `/import/rubrics`). `test:e2e` and a manual browser pass not yet run.

- **Stage 3 done** — `Team` → `Group`, one atomic PR. Migration `20260710000000_rename_team_to_group.ts` renames table `team` → `group` (a SQL reserved word — quoted in every raw-SQL reference; the schema/query builder quotes automatically), join table `student_to_team` → `student_to_group` (`team_id` → `group_id`), `submission.team_id` → `group_id`, and the `submission_type` enum **value** `'team'` → `'group'` via `renameValue` — the enum **type name** and `submission.type` column are untouched (stage 4's axis). Constraint/index renames via the schema builder and raw `ALTER INDEX`; **caught in review before landing:** `team_pkey` and `team_project_id_idx` (Postgres's default identity-column PK name and the per-table `add_projects`-created index) don't auto-rename with the table and needed explicit renames alongside it — same pattern as `question_pkey`/`question_project_id_idx` in 2b, initially missed on the first pass and found by inspecting the live schema after running the migration, not by static review. `submission_type_participant_check` is left unrenamed and untouched: verified empirically (not just asserted) that Postgres's column-attnum/enum-OID tracking auto-updates its displayed body to `group_id`/`'group'::submission_type` on both the column and enum-value rename, confirmed by inspecting `\d+ submission` post-migration and by a functional CHECK-violation test in a transaction. No trigger/enforcement-function work (neither `team`, `student_to_team`, nor `submission` carries one). Up/down/up round-trip run against the local dev Postgres (not a fresh testcontainer, since dev was already migrated through 2b) leaving zero stale `team`-named objects; regenerated Kysely types confirm `Group`, `StudentToGroup`, `SubmissionType = "group" | "individual"`. Two new migration-doc notes added to `docs/reference/database-migrations.md`: reserved-word identifiers need explicit quoting in raw SQL, and enum-value/column renames auto-update dependent CHECK bodies (trigger/function bodies do not — they're plain text). Tree-wide sweep of every `team`/`Team` identifier (locals, DB keys, string literals, error messages, comments) across 28 files in `src/submissions/`, `src/imports/{students,assessments,schemas,constants,types}`, `src/export/`, `src/assessment-capture/`, `src/db/constraints.integration.test.ts`, and `e2e/fixtures/`; mixed-noun identifiers moved only their group half (`teamMembersBySubmissionId` → `groupMembersBySubmissionId`, `submission` untouched). UI copy: `SubmissionSelector`'s placeholder/empty-state and `StudentsImportForm`'s description/help text moved team→group only, per Q5 grilling (single-axis — leaves "…grouped into the same submission" wording for the stage-7 UI-copy audit, since `submission` is stage 4/7's axis) — a mid-session reversal from an initial full-sentence-rewrite call. Cosmetic consistency rename `Team A` → `Group A` (and matching `team`/`Team` wording) across CSV placeholders/fixtures, `e2e/fixtures/expectations.ts`, and unit-test sample data, since leaving a fixture group literally named "Team A" right after retiring "team" would read as a leftover, not a decision. Validated: `check --fix`, `check-types`, `lint:boundaries`, full `vitest` (363/363, incl. integration against the local dev Postgres), production `build`, **`test:e2e`** (first e2e run since 2a — also exercises 2b's changes for the first time, per its handoff note that this gate was outstanding; passed against a fresh PG17 container), and a manual browser pass (Import Students page renders the renamed copy/placeholder; a real import writes to the `group` table and links `submission.group_id` correctly; the submissions CSV export shows `group,Group A,...`; test data cleaned up from the dev DB afterward). `.claude/launch.json` added (didn't exist) to enable the dev-server preview tooling for this and future stages.

- **Stage 4 done** — `Submission` → `Grade Target`, the #136 core, one atomic PR + one migration. Migration `20260711000000_rename_submission_to_grade_target.ts` renames table `submission` → `grade_target`, numeric `id` → `row_id`, adds the public **`id text`** (per-grid ordinal `t-<n>`, backfilled `'t-' || ROW_NUMBER() OVER (PARTITION BY project_id ORDER BY row_id)`, then `NOT NULL` + `UNIQUE (project_id, id)`), renames the three target-identity FK columns to the `_row_id` convention (`student_id` → `student_row_id`, `group_id` → `group_row_id`, and `assessment.submission_id` → `grade_target_row_id`), enum `submission_type` → `grade_target_kind`, column `type` → `kind`, and the full constraint/index set incl. the explicit `submission_pkey` → `grade_target_pkey` and `submission_project_id_idx` → `grade_target_project_id_idx` (the auto-rename landmine from 2b/3). The CHECK `submission_type_participant_check` → `grade_target_kind_participant_check` re-derived its body automatically (verified empirically via `\d+ grade_target` and a live CHECK-violation during cleanup). up/down/up round-trip run against dev Postgres with real fixture data across two grids, proving the per-grid backfill restarts at `t-1` for each grid (no global-count leak); schema builder throughout, one raw `ALTER INDEX` + one raw `UPDATE` for the backfill. Kysely types regenerated (`GradeTarget`, `GradeTargetKind = "group" | "individual"`, `assessment.gradeTargetRowId`).
  - **ID format settled while grilling 2026-07-10 (three iterations):** opaque `t-`+nanoid → bare integer → **`t-<n>` per-grid sequential text** (readable, honest, type-distinct from the integer `row_id`). Not opaque by design: a grid already gates access to its targets, and #136 only requires the id be non-derived, not unguessable. Generation is `COALESCE(MAX(substring(id FROM 3)::int), 0) + 1` per grid inside the import transaction (`nextGradeTargetIds` in `src/grade-targets/gradeTargets.ts`); reuse-after-deletion accepted (no counter column, no deletion path today, #45); the `UNIQUE (project_id, id)` index backstops concurrent-import races.
  - **Module + route rename.** Folder `src/submissions/` → `src/grade-targets/` (`submissions.ts` → `gradeTargets.ts`, `getSubmissionLabel` → `getGradeTargetLabel`, `Submission`/`SubmissionType` types → `GradeTarget`/`GradeTargetKind`, `loadSubmissions` → `loadGradeTargets`, new `toTargetSlug`). Export module `submissionExport*.ts` → `gradeTargetExport*.ts` (grades CSV reshaped: `submission_type`→`kind`, `submitter`→`name`, `grand_total_marks`→`final_total`, per-rubric bare `rubric.id` → `rubricId:total`). Routes to their **final shape** (retired the `assessments/` parent): `/grades`, `/grades/[targetId]/[targetSlug]`, `/grades/[targetId]/[targetSlug]/rubrics/[rubricId]`, `/results` at grid root, `/export/grades`; path helpers `projectGradesPath`/`projectGradeTargetPath`/`projectGradeTargetRubricPath`/`projectExportGradesPath` and `projectResultsPath` (dropped `/assessments`). Components `SubmissionAssessmentClient`/`SubmissionOverviewAssessmentClient`/`SubmissionSelector` → `GradeTarget*`; hooks `useSubmissionQuickJump` → `useGradeTargetQuickJump`, `submissionNavigation` → `gradeTargetNavigation`; the `assessment-completion` module's `*BySubmission` → `*ByTarget` throughout; cache tags `submissionListCacheTag` → `gradeTargetListCacheTag` (value `"grade-targets"`), `assessmentForSubmission*` → `assessmentForGradeTarget*` (keeping the `assessment` word — stage 5). `saveAssessment`'s params now carry `projectId` + `targetId` and resolve by `(projectId, targetId)` — required now that the public id is only grid-unique (CONTEXT Grid Resolution Strategy). `getProjectRouteContext`/CosmeticSlugReplacement unchanged in role, but the slug helper was **hardened to positional addressing** (`canonicalizeSlugSegment(pathname, idIndex, id, slug)`) so a bare `t-3` can't collide with an earlier path segment; a new `[targetId]/[targetSlug]/layout.tsx` mounts it. **`assessment` word untouched** everywhere (tables `assessment`/`criterion_assessment`, `saveAssessment`, `assessment-*` module names, `onAssess` props) — stage 5. `project`/`projectId` untouched — stage 6.
  - **Real bug fixed by the sweep:** `results/loadResults.ts` was selecting `assessment.submissionId` (the internal numeric key) and matching it against the target's public id in `resultsBuilder`; the join now goes through `gradeTarget` and selects the public `gradeTarget.id`, so the Grades matrix rows populate correctly (confirmed in the manual pass).
  - **Issue tracking:** this PR uses **`Related to #136`**, not `Closes` — #136's bullet-5 `project_id` → `grid_row_id` FK rename is completed in stage 6, which carries `Closes #136`.
  - Validated: `check --fix`, `check-types`, `lint:boundaries`, full `vitest` (366/366, incl. integration against dev Postgres), production `build` (routes render as `/grades/[targetId]/[targetSlug]`, `/results`, `/export/grades`), **`test:e2e`** (fresh PG17 container, happy-path import→grade→persist→export green), and a **manual browser pass** (create/import → Grades roster → grade a criterion at `/grades/t-2/doe-john` → reload-persists → Previous/Next + Lookup-jump navigation → Results tables → `export/grades` CSV shows `kind,name,q1:r1,q1:total,q2:r2,q2:total,final_total`; dev test data cleaned up afterward). `.claude/launch.json` gained `"autoPort": true` so the preview server survives port 3000 being taken.

## Out of scope

- Structural model changes deferred by the investigations: unifying Student/Group under a singleton-Group persistence model (assessment target model investigation), the aggregate-Total computation itself (mark/grade/weighting investigation — Total is named but unbuilt), dynamic target creation (#61), draft/unresolved target state.
- Cross-grid aggregation of a real-world event split across grids (documented limitation, not a defect).
